const initialState = {
    posts: [],
    loading: true
}

const postsReducer = (state, action) => {
    switch(action.type) {
        default:
            return state
    }
}

export default postsReducer